[pytest]
asyncio_mode = auto
# Pools are created per test, so fixtures share the test's own loop.
asyncio_default_fixture_loop_scope = function
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning

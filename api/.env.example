# Copy to api/.env for running the API outside Docker.
# The password is the ABCD_APP_PASSWORD from the project-root .env.
DATABASE_URL=postgresql://abcd_app:CHANGE_ME@localhost:5432/abcd

# Comma-separated. The Vite dev server runs on 5173.
# CORS_ORIGINS=http://localhost:5173

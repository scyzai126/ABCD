# Copy to .env and fill in. Both values are read by docker-compose.yml.
# POSTGRES_PASSWORD -> the `postgres` superuser (admin/migrations only).
# ABCD_APP_PASSWORD -> the `abcd_app` read-only role the web app connects as.
POSTGRES_PASSWORD=change-me
ABCD_APP_PASSWORD=change-me-too
